<%= form_for @fornecedor, :html => { :class => "form-horizontal" } do |f| %>
  <% if @fornecedor.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title">
          <%= pluralize(@fornecedor.errors.count, "error") %> prohibited this 
          fornecedor from being saved:
        </h3>
      </div>
      <div class="panel-body">
        <ul><% fornecedor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %></ul>
      </div>
    </div>
  <% end %>

      <div class="field">
      <%= f.label :nome, :class => 'control-label col-lg-2' %>
      <div class="col-lg-10">
        <%= f.text_field :nome, :class => 'form-control' %>
      </div>
    </div>
      <div class="field">
      <%= f.label :ativo, :class => 'control-label col-lg-2' %>
      <div class="col-lg-10">
        <%= f.check_box :ativo, :class => 'form-control' %>
      </div>
    </div>
    <div class="actions">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
    </div>
  </div>
<% end %>
